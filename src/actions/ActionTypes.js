/*
    Declare the types of actions
    by adding "export" in front, when it will be loaded,
    we can write - import * as types from '.ActionTypes' 
*/

export const INCREMENT = 'INCREMENT';
export const DECREMENT = 'DECREMENT';
export const SET_COLOR = 'SET_COLOR';